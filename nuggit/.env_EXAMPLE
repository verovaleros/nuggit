# GitHub API Configuration
# Get your token from: https://github.com/settings/tokens
# Required scopes: public_repo (for public repositories) or repo (for private repositories)
GITHUB_TOKEN=

# Authentication Configuration
# Generate a secure secret key for JWT tokens (use a long, random string)
# You can generate one with: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=10080
REFRESH_TOKEN_EXPIRE_DAYS=30

# Email Configuration (Required for user registration and password reset)
# These settings are required for the authentication system to work
FROM_EMAIL=nuggit@yourdomain.com
FROM_NAME=Nuggit
SMTP_HOST=your-smtp-server.com
SMTP_PORT=587
SMTP_USERNAME=your-smtp-username
SMTP_PASSWORD=your-smtp-password
SMTP_USE_TLS=true
SMTP_USE_SSL=false

# Application Configuration
BASE_URL=http://localhost:5173

# Optional: Support Link
KO_FI_URL=https://ko-fi.com/yourusername
